# Write a program to return/print the Python version you are using. DONE
import sys
import platform

print(sys.version)
print(platform.python_version())

# Can also be done in command line using "python --version"